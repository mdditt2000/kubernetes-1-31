apiVersion: v1
kind: Service
metadata:
  name: primary-endpoint
  labels:
    app: primary-endpoint
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: primary-endpoint
  type: NodePort